{
  "name": "keycloak-bot",
  "version": "0.3.1",
  "main": "dist/index.js",
  "scripts": {
    "dist": "npx -p @vercel/ncc ncc build index.js",
    "format": "prettier --write .",
    "check-format": "prettier --check ."
  },
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "octokit": "^2.0.10"
  },
  "devDependencies": {
    "prettier": "^2.8.1",
    "prettier-cli": "^0.1.0"
  }
}
